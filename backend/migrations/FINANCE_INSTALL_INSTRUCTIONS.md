# Установка системы финансов

## Шаг 1: Создать таблицы (уже выполнено ✅)
Файл: `create_finance_system.sql` (первые 6 запросов)

## Шаг 2: Создать триггеры

### В phpMyAdmin:

1. Открой базу данных `creative_collective`
2. Нажми на вкладку **SQL** вверху
3. Скопируй **ВЕСЬ КОД** из файла `create_finance_triggers.sql`
4. Вставь в поле SQL
5. Нажми **Выполнить**

### Важно:
- Код нужно вставить **ЦЕЛИКОМ** (вместе с `DELIMITER $$` и `DELIMITER ;`)
- phpMyAdmin сам обработает DELIMITER правильно

## Проверка:

После выполнения должно быть:
- ✅ 3 таблицы: `transactions`, `user_balances`, `withdrawal_requests`
- ✅ 2 триггера: `after_transaction_insert`, `after_transaction_update`

### Проверить триггеры:

```sql
SHOW TRIGGERS FROM creative_collective;
```

Должно показать 2 триггера.

## Готово! 🎉

После этого система финансов полностью работает!

