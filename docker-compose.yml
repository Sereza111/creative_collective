version: '3.8'

services:
  # Node.js API Server - подключается к внешней БД на Beget
  api:
    build:
      context: ./backend
      dockerfile: Dockerfile
    image: creative_collective_api:latest
    container_name: creative_collective_api
    restart: unless-stopped
    ports:
      - "8080:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
      DB_HOST: eskopeboyed.beget.app
      DB_PORT: 3306
      DB_USER: creative_collective
      DB_PASSWORD: tVog4KLomh!l
      DB_NAME: creative_collective
      JWT_SECRET: creative_collective_secret_key_2025_change_in_production
      JWT_EXPIRES_IN: 7d
      JWT_REFRESH_SECRET: creative_collective_refresh_secret_2025
      JWT_REFRESH_EXPIRES_IN: 30d
      CORS_ORIGIN: "*"
