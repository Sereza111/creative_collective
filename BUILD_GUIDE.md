# 🚀 Как запустить приложение

## 📱 Вариант 1: Windows Desktop (РЕКОМЕНДУЕТСЯ)

Это самый простой способ для тестирования:

```bash
# Запустить на Windows
flutter run -d windows
```

---

## 🌐 Вариант 2: В браузере Chrome

Быстрый запуск в браузере:

```bash
# Запустить в Chrome
flutter run -d chrome
```

---

## 📱 Вариант 3: Android эмулятор

Если хотите протестировать на Android:

### Шаг 1: Проверить доступные эмуляторы
```bash
flutter emulators
```

### Шаг 2: Запустить эмулятор (если есть)
```bash
# Запустить эмулятор по имени
flutter emulators --launch <emulator_id>
```

### Шаг 3: Запустить приложение
```bash
# После того как эмулятор загрузится
flutter run
```

---

## 🔨 Создать APK для установки на реальный телефон

```bash
# Создать debug APK (для тестирования)
flutter build apk --debug

# Файл будет в: build/app/outputs/flutter-apk/app-debug.apk
```

```bash
# Создать release APK (финальная версия)
flutter build apk --release

# Файл будет в: build/app/outputs/flutter-apk/app-release.apk
```

---

## 🎯 Быстрый старт (РЕКОМЕНДУЮ)

1. **Запустить на Windows прямо сейчас:**
   ```bash
   flutter run -d windows
   ```

2. **Горячая перезагрузка во время работы:**
   - Нажми `r` в консоли - быстрая перезагрузка
   - Нажми `R` в консоли - полная перезагрузка
   - Нажми `q` в консоли - выход

3. **Тестовые данные для входа:**
   ```
   Email: admin@creative.local
   Password: admin123
   ```

---

## 🐛 Если возникли проблемы

### Очистить кэш и пересобрать:
```bash
flutter clean
flutter pub get
flutter run -d windows
```

### Проверить ошибки:
```bash
flutter analyze
```

---

## 📊 Что можно протестировать

✅ **Авторизация**
- Вход через admin@creative.local / admin123
- Регистрация нового пользователя

✅ **Главный экран**
- Статистика по задачам и проектам
- Pull-to-refresh (потянуть вниз)
- Кнопка Dashboard

✅ **Задачи**
- Просмотр всех задач
- Фильтрация (Все/Ожидает/В работе/Завершено)
- ➕ Создание новой задачи
- Pull-to-refresh

✅ **Проекты**
- Просмотр всех проектов
- Фильтрация (Все/Планирование/Активные/Завершенные)
- ➕ Создание нового проекта
- Pull-to-refresh

✅ **Выход**
- Кнопка выхода в AppBar на главном экране

---

## 🎨 Горячие клавиши во время работы

Когда приложение запущено, в консоли можно использовать:

- `r` - Hot reload (быстрая перезагрузка, сохраняет состояние)
- `R` - Hot restart (полная перезагрузка)
- `h` - Показать все команды
- `q` - Выход из приложения
- `d` - Открыть DevTools
- `w` - Показать дерево виджетов в консоли

---

## 💡 Советы

1. **Первый запуск может быть медленным** (1-3 минуты) - это нормально, Flutter компилирует код
2. **Hot reload** работает очень быстро - изменения применяются за 1-2 секунды
3. **Windows версия** самая быстрая для разработки
4. **Chrome версия** удобна для быстрого тестирования UI
5. **Android APK** нужен для установки на реальный телефон

---

**Готово к запуску! 🚀**

